apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: web-napbiotec-v2
spec:
  generators:
  - clusters:
      selector:
        matchLabels:
          custom: "true"
  template:
    metadata:
      name: 'web-napbiotec-v2-{{name}}'
    spec:
      project: "default"
      source:
        repoURL: git@github.com:nap-devops/nap-devops-argocd-apps.git
        targetRevision: "{{name}}" #Based on environment
        path: 'manifests/web-napbiotec-v2'
      destination:
        name: in-cluster
        namespace: web-napbiotec-v2
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
        syncOptions:
          - CreateNamespace=true
